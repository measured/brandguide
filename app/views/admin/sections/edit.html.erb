<h1>Edit <%= @brand_guide.title %> / <%= @section.title %></h1>

<%= simple_form_for [:admin, @brand_guide, @section], html: { multipart: true } do |f| %>
  <%= f.error_notification %>

  <%= f.input :title %>
  <%= f.input :content %>

  <%= f.input :files, label: 'Add Assets', as: :file, input_html: { multiple: true } %>

  <%= f.button :submit %>
<% end %>